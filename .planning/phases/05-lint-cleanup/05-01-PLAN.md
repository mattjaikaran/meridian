---
phase: 05-lint-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/db.py
  - scripts/generate_commands.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02

must_haves:
  truths:
    - "ruff check scripts/db.py --select E501 returns zero violations"
    - "ruff check scripts/generate_commands.py --select E501 returns zero violations"
    - "All 338 existing tests still pass unchanged"
  artifacts:
    - path: "scripts/db.py"
      provides: "E501-clean SQL schema definitions"
      contains: "entity_type TEXT NOT NULL CHECK"
    - path: "scripts/generate_commands.py"
      provides: "E501-clean command generator"
  key_links:
    - from: "scripts/db.py"
      to: "tests/"
      via: "SQL schema used by test fixtures"
      pattern: "CREATE TABLE"
    - from: "scripts/generate_commands.py"
      to: "tests/"
      via: "generated output validated by tests"
---

<objective>
Fix all E501 (line too long) ruff violations in scripts/db.py and scripts/generate_commands.py.

Purpose: Achieve zero lint violations across all Python source files for v1.1 code quality baseline.
Output: Both files pass `ruff check --select E501` with zero errors, all tests still pass.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix E501 in scripts/db.py (QUAL-01)</name>
  <files>scripts/db.py</files>
  <action>
Fix line 161 which is 120 chars (limit 100). The line is a SQL CHECK constraint:

```
entity_type TEXT NOT NULL CHECK (entity_type IN ('milestone','phase','plan','quick_task','nero_dispatch','review')),
```

Break the CHECK constraint value list across multiple lines while preserving valid SQL. Use a multi-line parenthesized expression, for example:

```sql
entity_type TEXT NOT NULL CHECK (entity_type IN (
    'milestone','phase','plan','quick_task',
    'nero_dispatch','review'
)),
```

This is inside a Python triple-quoted string (SQL DDL). Ensure the SQL remains syntactically valid. Do NOT change any column names, types, or constraint logic.
  </action>
  <verify>
    <automated>ruff check scripts/db.py --select E501 && echo "PASS: zero E501 violations"</automated>
  </verify>
  <done>ruff check scripts/db.py --select E501 returns zero violations</done>
</task>

<task type="auto">
  <name>Task 2: Fix E501 in scripts/generate_commands.py (QUAL-02)</name>
  <files>scripts/generate_commands.py</files>
  <action>
Fix 2 E501 violations in the f-string template that generates documentation content:

Line 174 (174 chars): Long description string starting with "Meridian is a SQLite-backed state machine..."
Line 182 (168 chars): Long description starting with "State is stored in..."

Both are inside an f-string triple-quoted block (content = f"""..."""). Break each long line by inserting a newline at a natural sentence boundary or phrase break so no single line exceeds 100 chars. Since this is a documentation string rendered as markdown, line breaks within a paragraph are fine (markdown will join them).

Do NOT change the actual wording or meaning of the text. Just reflow to stay under 100 chars per line.
  </action>
  <verify>
    <automated>ruff check scripts/generate_commands.py --select E501 && echo "PASS: zero E501 violations"</automated>
  </verify>
  <done>ruff check scripts/generate_commands.py --select E501 returns zero violations</done>
</task>

</tasks>

<verification>
1. `ruff check scripts/db.py --select E501` -- zero violations
2. `ruff check scripts/generate_commands.py --select E501` -- zero violations
3. `uv run pytest` -- all 338 tests pass (no functional regressions)
</verification>

<success_criteria>
- Both files have zero E501 violations
- Full test suite passes with no changes to test files
- No functional behavior changes (only line wrapping)
</success_criteria>

<output>
After completion, create `.planning/phases/05-lint-cleanup/05-01-SUMMARY.md`
</output>
