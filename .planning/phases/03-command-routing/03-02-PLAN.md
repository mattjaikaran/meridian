---
phase: 03-command-routing
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - SKILL.md
autonomous: false
requirements:
  - ROUT-01
  - ROUT-04
must_haves:
  truths:
    - "Running the generator installs all 13 command files in ~/.claude/commands/meridian/"
    - "Root SKILL.md no longer has a Commands section"
    - "Typing /meridian: in Claude Code shows all 13 subcommands in autocomplete"
  artifacts:
    - path: "~/.claude/commands/meridian/init.md"
      provides: "Example generated command wrapper"
      contains: "meridian:generated"
    - path: "SKILL.md"
      provides: "Root SKILL.md passive context (no Commands section)"
  key_links:
    - from: "~/.claude/commands/meridian/*.md"
      to: "~/.claude/skills/meridian/skills/*/SKILL.md"
      via: "@ file reference in wrapper body"
      pattern: "@/Users/mattjaikaran/.claude/skills/meridian"
---

<objective>
Run the generator to install all command files and verify end-to-end in Claude Code.

Purpose: The generator is built and tested (Plan 01). Now run it for real to install commands, update root SKILL.md, and verify the user can discover all 13 commands via `/meridian:` autocomplete.

Output: 13 command files in `~/.claude/commands/meridian/`, updated root `SKILL.md`, manual verification
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-command-routing/03-01-SUMMARY.md
@.planning/phases/03-command-routing/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run generator and commit root SKILL.md</name>
  <files>SKILL.md</files>
  <action>
    Run the generator script to install all command files and update root SKILL.md:

    1. Run `uv run python scripts/generate_commands.py` — this creates `~/.claude/commands/meridian/` with all 13 command wrappers and regenerates root SKILL.md
    2. Verify the output summary shows "Created: 13" and "Skipped: 0"
    3. Verify `~/.claude/commands/meridian/` contains exactly 13 `.md` files (checkpoint.md, dashboard.md, debug.md, dispatch.md, execute.md, init.md, plan.md, quick.md, resume.md, review.md, roadmap.md, ship.md, status.md)
    4. Spot-check one generated file (e.g., init.md) to confirm:
       - First line is `<!-- meridian:generated -->`
       - Has correct frontmatter with `name: meridian:init`
       - Body has `@/Users/mattjaikaran/.claude/skills/meridian/skills/init/SKILL.md`
    5. Verify root SKILL.md no longer contains `## Commands` section
    6. Verify root SKILL.md still has Architecture, Scripts, References sections
    7. Commit the updated SKILL.md (command files in ~/.claude/ are outside the repo)

    If the generator warns about symlink issues, run with `--fix-symlink` to repair.
  </action>
  <verify>
    <automated>ls ~/.claude/commands/meridian/*.md | wc -l && grep -c "meridian:generated" ~/.claude/commands/meridian/init.md && ! grep -q "## Commands" SKILL.md && echo "PASS"</automated>
  </verify>
  <done>13 command files installed in ~/.claude/commands/meridian/, root SKILL.md updated to passive context, SKILL.md committed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify /meridian:* commands in Claude Code</name>
  <files>~/.claude/commands/meridian/*.md</files>
  <action>
    Human verifies all 13 commands are discoverable in Claude Code autocomplete.

    What was built: All 13 Meridian slash commands installed as /meridian:* in Claude Code, plus updated root SKILL.md for passive context.

    How to verify:
    1. Open a new Claude Code session (or type /clear in current one to refresh command cache)
    2. Type `/meridian:` and verify autocomplete shows all 13 subcommands: init, plan, execute, resume, status, dashboard, roadmap, dispatch, review, ship, debug, quick, checkpoint
    3. Select one command (e.g., `/meridian:status`) and verify it loads the skill procedure correctly (should show the SKILL.md content)
    4. Verify no duplicate or conflicting command entries appear
    5. Check that the root SKILL.md still provides passive project context (architecture, scripts, references sections visible when Claude loads the meridian skill)

    Known risk: If the @ file reference through the symlink does not resolve, Claude will say it cannot find the file. In that case, we need to adjust the reference path format in the generator.

    Resume signal: Type "approved" if all 13 commands appear and work, or describe any issues.
  </action>
  <verify>Manual verification in Claude Code session</verify>
  <done>User confirms /meridian: autocomplete shows all 13 subcommands and at least one command loads its SKILL.md procedure correctly</done>
</task>

</tasks>

<verification>
- `ls ~/.claude/commands/meridian/*.md | wc -l` returns 13
- `grep -l "meridian:generated" ~/.claude/commands/meridian/*.md | wc -l` returns 13
- `grep "## Commands" SKILL.md` returns no matches
- `uv run pytest tests/test_generate_commands.py -x` passes
- Manual: `/meridian:` autocomplete shows all 13 commands in Claude Code
</verification>

<success_criteria>
- All 13 command files exist in `~/.claude/commands/meridian/`
- Each command file has correct frontmatter and @ reference
- Root SKILL.md is passive (no Commands section)
- User confirms `/meridian:` autocomplete works in Claude Code
</success_criteria>

<output>
After completion, create `.planning/phases/03-command-routing/03-02-SUMMARY.md`
</output>
