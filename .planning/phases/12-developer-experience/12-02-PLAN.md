---
phase: 12-developer-experience
plan: 02
type: feature
wave: 1
depends_on: []
files_modified:
  - scripts/backlog.py
  - skills/seed/SKILL.md
  - tests/test_backlog.py
autonomous: true
requirements:
  - DX-02

must_haves:
  truths:
    - "/meridian:seed captures a forward-looking idea with optional trigger conditions"
    - "Seeds are stored in .meridian/backlog.md with trigger metadata"
    - "Seeds can be listed, promoted to phases, or dismissed"
    - "Seeds with trigger conditions surface automatically when conditions are met"
  artifacts:
    - path: "scripts/backlog.py"
      provides: "Backlog/seed management with trigger conditions"
      exports:
        - "plant_seed"
        - "list_seeds"
        - "promote_seed"
        - "dismiss_seed"
        - "check_triggers"
    - path: "skills/seed/SKILL.md"
      provides: "/meridian:seed skill definition"
    - path: "tests/test_backlog.py"
      provides: "Unit tests for backlog operations"
      min_lines: 50
---

<objective>
Build /meridian:seed for backlog parking lot with trigger conditions.

Seeds are ideas that aren't ready for planning yet but should surface later:
- `plant "Add caching layer" --trigger "after phase Auth completes"`
- `list` — show all seeds with status
- `promote <id>` — convert seed to a phase in current milestone
- `dismiss <id>` — archive seed as not needed

Trigger types:
- `after_phase:<phase_name>` — surface when named phase completes
- `after_milestone:<name>` — surface at milestone boundary
- `manual` — only surfaces when explicitly listed

Storage: `.meridian/backlog.md` with YAML-like metadata per seed.
</objective>

<tasks>
- task: Create `scripts/backlog.py` with seed CRUD and trigger checking
  wave: 1
  verify: "uv run pytest tests/test_backlog.py -x"

- task: Create `skills/seed/SKILL.md`
  wave: 1
  verify: "test -f skills/seed/SKILL.md"

- task: Write tests for plant, list, promote, dismiss, and trigger matching
  wave: 1
  verify: "uv run pytest tests/test_backlog.py -v"
</tasks>
