---
phase: 12-developer-experience
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - scripts/profiler.py
  - skills/profile/SKILL.md
  - tests/test_profiler.py
autonomous: true
requirements:
  - DX-01

must_haves:
  truths:
    - "/meridian:profile analyzes project history to build a developer preference profile"
    - "Profile captures: preferred patterns, tech choices, commit style, review preferences"
    - "Profile is saved to .meridian/USER-PROFILE.md for future session context"
    - "Profile can be refreshed with --refresh flag"
  artifacts:
    - path: "scripts/profiler.py"
      provides: "Developer profiling from git history and project patterns"
      exports:
        - "analyze_project_patterns"
        - "generate_profile"
        - "save_profile"
    - path: "skills/profile/SKILL.md"
      provides: "/meridian:profile skill definition"
    - path: "tests/test_profiler.py"
      provides: "Unit tests for profiling logic"
      min_lines: 40
---

<objective>
Build /meridian:profile for developer preference profiling.

Analyzes the project to extract patterns:
1. Git log: commit message style, frequency, branch naming
2. Code patterns: language mix, framework usage, test style
3. Project structure: directory conventions, config files present
4. Generate a USER-PROFILE.md with structured observations
5. Profile informs future plan generation and review criteria
</objective>

<tasks>
- task: Create `scripts/profiler.py` with analysis and profile generation
  wave: 1
  verify: "uv run pytest tests/test_profiler.py -x"

- task: Create `skills/profile/SKILL.md`
  wave: 1
  verify: "test -f skills/profile/SKILL.md"

- task: Write tests for pattern extraction and profile generation
  wave: 1
  verify: "uv run pytest tests/test_profiler.py -v"
</tasks>
