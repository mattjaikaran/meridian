---
phase: 01-database-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - scripts/sync.py
  - scripts/resume.py
  - scripts/axis_sync.py
  - scripts/export.py
  - scripts/dispatch.py
  - scripts/context_window.py
autonomous: true
requirements:
  - DBRL-05

must_haves:
  truths:
    - "No script has manual connect/try/finally/close -- all use open_project()"
    - "No script imports connect from db (only open_project)"
    - "All existing script functionality preserved (same CLI behavior)"
  artifacts:
    - path: "scripts/sync.py"
      provides: "Sync script using open_project"
      contains: "open_project"
    - path: "scripts/resume.py"
      provides: "Resume script using open_project"
      contains: "open_project"
    - path: "scripts/axis_sync.py"
      provides: "Axis sync script using open_project"
      contains: "open_project"
    - path: "scripts/export.py"
      provides: "Export script using open_project"
      contains: "open_project"
    - path: "scripts/dispatch.py"
      provides: "Dispatch script using open_project"
      contains: "open_project"
    - path: "scripts/context_window.py"
      provides: "Context window script using open_project"
      contains: "open_project"
  key_links:
    - from: "scripts/sync.py"
      to: "scripts/db.py"
      via: "imports open_project instead of connect"
      pattern: "from scripts\\.db import open_project"
    - from: "scripts/dispatch.py"
      to: "scripts/db.py"
      via: "imports open_project instead of connect"
      pattern: "from scripts\\.db import open_project"
---

<objective>
Migrate all 6 scripts with manual connection management to use open_project() context manager. Eliminate every manual connect/try/finally/close pattern from the codebase.

Purpose: DBRL-05 requires all existing scripts use open_project(). This is a mechanical refactor -- each script's __main__ block and internal functions that create connections get converted to `with open_project(...) as conn:` blocks.

Output: All 6 scripts (sync, resume, axis_sync, export, dispatch, context_window) use open_project(). Zero instances of manual conn.close() remain in scripts/.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-database-foundation/01-CONTEXT.md
@.planning/phases/01-database-foundation/01-RESEARCH.md
@.planning/phases/01-database-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: new db.py public API -->
From scripts/db.py (after Plan 01):
```python
@contextlib.contextmanager
def open_project(path: str | Path | None = None):
    """Yields configured connection. Auto-commit on success, rollback on exception, close in finally."""
    ...

def get_db_path(project_dir: str | Path | None = None) -> Path: ...
def init_schema(conn: sqlite3.Connection, db_path: Path | None = None) -> None: ...
def get_schema_version(conn: sqlite3.Connection) -> int: ...
def backup_database(source_path: Path, backup_dir: Path | None = None, max_backups: int = 100) -> Path: ...
def retry_on_busy(max_retries: int = 3, base_delay: float = 0.5): ...

class DatabaseBusyError(Exception): ...
```

<!-- Pattern for converting scripts (from RESEARCH.md) -->
BEFORE:
```python
conn = connect(get_db_path(project_dir))
try:
    # ... work ...
finally:
    conn.close()
```

AFTER:
```python
with open_project(project_dir) as conn:
    # ... work ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert all 6 scripts from manual connection management to open_project()</name>
  <files>scripts/sync.py, scripts/resume.py, scripts/axis_sync.py, scripts/export.py, scripts/dispatch.py, scripts/context_window.py</files>
  <action>
For each of the 6 scripts, apply this mechanical transformation:

**scripts/sync.py** (1 connection site at line ~245):
- Replace `from scripts.db import connect, get_db_path` with `from scripts.db import open_project`
- Convert the `conn = connect(get_db_path(project_dir))` / `try/finally/conn.close()` to `with open_project(project_dir) as conn:`
- Keep all logic inside the with block unchanged

**scripts/resume.py** (1 connection site at line ~81 and ~249):
- Replace connect/get_db_path imports with open_project
- Convert the `conn = connect(db_path)` / `try/finally/conn.close()` patterns to `with open_project(...):`
- Note: resume.py may derive db_path then connect -- use `open_project(project_dir)` where project_dir is the directory, or `open_project(db_path)` if passing a direct path. Check what get_db_path returns vs what connect expects.

**scripts/axis_sync.py** (2 connection sites at lines ~60 and ~113):
- Replace imports, convert both connection patterns to open_project()
- Two separate functions each create their own connection -- each gets its own `with open_project():`

**scripts/export.py** (2 connection sites at lines ~27 and ~76):
- Replace imports, convert both patterns
- Two functions: one for JSON export, one for the __main__ CLI

**scripts/dispatch.py** (3 connection sites at lines ~31, ~135, ~170):
- Replace imports, convert all 3 patterns
- Three separate functions each managing connections

**scripts/context_window.py** (check for connection patterns):
- Replace any connect/get_db_path usage with open_project

For EACH script:
1. Update imports: remove `connect` (and `get_db_path` if only used for connect). Add `open_project`. Keep `get_db_path` import if used for other purposes (like path resolution outside of connection).
2. Find every `conn = connect(...)` paired with `conn.close()` in a finally block
3. Replace with `with open_project(...) as conn:` and indent the body
4. Remove the try/finally/conn.close() boilerplate
5. Do NOT change any business logic, function signatures, or state.py function calls

CRITICAL: Some scripts use `get_db_path(project_dir)` to resolve the path, then pass it to `connect()`. Since `open_project()` accepts either a project directory or a db path (it calls `get_db_path` internally if needed), pass the project_dir directly to `open_project()`. Check the `open_project` implementation from Plan 01 to confirm the parameter convention.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/meridian && grep -r "conn\.close()" scripts/ --include="*.py" | grep -v __pycache__ | grep -v "_connect" | wc -l | xargs test 0 -eq && echo "PASS: no manual conn.close() remaining"</automated>
  </verify>
  <done>All 6 scripts use open_project() exclusively. Zero manual conn.close() calls remain in scripts/. Zero imports of connect from db.py in any script.</done>
</task>

<task type="auto">
  <name>Task 2: Validate full codebase -- all tests pass, no regressions</name>
  <files>scripts/db.py</files>
  <action>
Run full validation suite to confirm the migration introduced no regressions:

1. Run `uv run pytest tests/ -v` -- all tests must pass (including the new test_db.py from Plan 01 and all existing tests).

2. Run codebase-wide checks:
   - `grep -r "from scripts.db import connect" scripts/ --include="*.py" | grep -v __pycache__` must return empty (no script imports connect)
   - `grep -r "conn.close()" scripts/ --include="*.py" | grep -v __pycache__` must return empty (no manual close)
   - `grep -r "from scripts.db import.*connect" scripts/ --include="*.py" | grep -v __pycache__` must return empty or only show _connect internal usage

3. If any test fails, diagnose and fix. Common issues:
   - Import errors: script imported connect but it is now _connect -- fix import to use open_project
   - Path resolution: open_project may need the project_dir, not db_path -- check what each script was passing to get_db_path/connect
   - Missing init_schema call: open_project calls init_schema internally -- scripts that called init_schema separately may now run it twice (harmless due to CREATE IF NOT EXISTS, but verify)

4. Run `uv run ruff check scripts/` to verify no linting issues introduced.

5. If any script has a simple smoke-test command documented in its __main__ block, run it to verify CLI behavior is preserved.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/meridian && uv run pytest tests/ -v && echo "ALL TESTS PASS"</automated>
  </verify>
  <done>All tests pass. No script imports connect. No script has manual conn.close(). Ruff reports no issues. Full codebase uses open_project() as the single database access pattern.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -v` -- all tests green
- `grep -rn "conn\.close()" scripts/ --include="*.py" | grep -v __pycache__` -- returns nothing
- `grep -rn "from scripts.db import connect" scripts/ --include="*.py" | grep -v __pycache__` -- returns nothing
- `grep -rn "open_project" scripts/ --include="*.py" | grep -v __pycache__ | wc -l` -- at least 7 matches (6 scripts + db.py definition)
</verification>

<success_criteria>
- All 6 scripts (sync, resume, axis_sync, export, dispatch, context_window) use open_project()
- Zero manual connect/try/finally/close patterns remain anywhere in scripts/
- All existing tests pass without modification (fixture migration was done in Plan 01)
- Ruff linting clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-foundation/01-02-SUMMARY.md`
</output>
