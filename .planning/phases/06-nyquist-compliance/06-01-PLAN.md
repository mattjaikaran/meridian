---
phase: 06-nyquist-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/nyquist.py
  - tests/test_nyquist.py
  - scripts/state.py
autonomous: true
requirements:
  - COMP-01

must_haves:
  truths:
    - "After a wave completes during execution, VALIDATION.md frontmatter contains wave_N_complete and wave_N_validated fields with actual results"
    - "Test commands are parsed from VALIDATION.md body (Quick run command and Full suite command rows)"
    - "nyquist_compliant flips to true only when all waves pass"
  artifacts:
    - path: "scripts/nyquist.py"
      provides: "VALIDATION.md parser, test runner, frontmatter updater"
      exports: ["parse_validation_md", "run_wave_validation", "update_validation_frontmatter"]
    - path: "tests/test_nyquist.py"
      provides: "Unit tests for nyquist module"
      min_lines: 60
    - path: "scripts/state.py"
      provides: "Hook calling nyquist validation on wave completion"
      contains: "run_wave_validation"
  key_links:
    - from: "scripts/state.py"
      to: "scripts/nyquist.py"
      via: "import and call after wave completion in check_auto_advance"
      pattern: "run_wave_validation"
    - from: "scripts/nyquist.py"
      to: ".planning/phases/*/VALIDATION.md"
      via: "parse frontmatter + body, write updated frontmatter"
      pattern: "parse_validation_md"
---

<objective>
Build the Nyquist validation engine: parse VALIDATION.md test commands, run them on wave completion, and update frontmatter with actual pass/fail results.

Purpose: COMP-01 requires that VALIDATION.md frontmatter reflects real execution results, not stale placeholders. This plan creates the engine that makes that happen automatically during plan execution.
Output: scripts/nyquist.py with parse/run/update functions, tests, and state.py integration hook.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-nyquist-compliance/06-CONTEXT.md

@scripts/state.py
@scripts/validate.py
@.planning/phases/05-lint-cleanup/05-VALIDATION.md
@.planning/phases/01-database-foundation/01-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From scripts/state.py:
```python
def check_auto_advance(conn: sqlite3.Connection, phase_id: int) -> dict:
    """Called after plan completion. Returns {action, message}."""

def transition_plan(conn: sqlite3.Connection, plan_id: int, new_status: str, ...) -> dict:
    """Handles plan state changes."""

def list_plans(conn: sqlite3.Connection, phase_id: int) -> list[dict]:
    """Fetch all plans for a phase, ordered by wave, sequence."""

def get_plans_by_wave(conn: sqlite3.Connection, phase_id: int, wave: int) -> list[dict]:
    """Fetch plans for a specific wave."""
```

VALIDATION.md frontmatter structure (current):
```yaml
---
phase: N
slug: phase-slug
status: draft
nyquist_compliant: false
wave_0_complete: false
created: YYYY-MM-DD
---
```

VALIDATION.md body pattern for test commands:
```markdown
| **Quick run command** | `uv run pytest tests/ -x -q` |
| **Full suite command** | `uv run pytest tests/ -v` |
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create nyquist.py validation engine with tests</name>
  <files>scripts/nyquist.py, tests/test_nyquist.py</files>
  <behavior>
    - parse_validation_md(phase_dir: Path) returns dict with frontmatter fields and extracted test commands (quick_command, full_command)
    - parse_validation_md handles missing VALIDATION.md (returns None)
    - parse_validation_md handles malformed frontmatter (returns partial dict with error field)
    - run_wave_validation(phase_dir: Path, wave: int, repo_path: str = ".") runs the full suite command from VALIDATION.md body, returns {wave: int, passed: bool, command: str, output: str, validated_at: str}
    - update_validation_frontmatter(phase_dir: Path, wave_results: dict) writes updated frontmatter with wave_N_complete, wave_N_validated fields; sets nyquist_compliant=true only if ALL wave fields are true; updates status from "draft" to "validated" (all pass) or "failed" (any fail)
    - update_validation_frontmatter preserves all existing frontmatter fields and the full body content
  </behavior>
  <action>
    Write tests first in tests/test_nyquist.py:
    - Use tmp_path fixture to create fake VALIDATION.md files with known frontmatter and body content
    - Test parse_validation_md extracts Quick/Full run commands from the markdown table format
    - Test parse_validation_md returns None for missing file
    - Test run_wave_validation with a trivial command (e.g., "true" or "echo ok") to verify result structure
    - Test update_validation_frontmatter writes correct YAML fields and preserves body
    - Test nyquist_compliant logic: false when any wave fails, true only when all pass

    Then implement scripts/nyquist.py:
    - parse_validation_md: Read VALIDATION.md, split frontmatter (between --- markers) from body. Parse YAML frontmatter. Regex-extract test commands from body table rows matching "Quick run command" and "Full suite command" patterns.
    - run_wave_validation: Call parse_validation_md to get the full suite command. Run it via subprocess.run(cmd, shell=True, capture_output=True, cwd=repo_path, timeout=120). Return structured result dict.
    - update_validation_frontmatter: Read existing file. Parse frontmatter. Merge wave results (wave_N_complete: bool, wave_N_validated: timestamp). Compute nyquist_compliant (all wave_*_complete fields must be true). Set status to "validated" or "failed". Write back frontmatter + original body.

    Use standard library only (yaml parsing via simple regex since frontmatter is simple key: value pairs — avoid adding PyYAML dependency). Use the same pattern as validate.py for subprocess calls.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/meridian && uv run pytest tests/test_nyquist.py -x -v</automated>
  </verify>
  <done>parse_validation_md correctly extracts frontmatter and test commands from VALIDATION.md files. run_wave_validation executes commands and returns structured results. update_validation_frontmatter writes per-wave results and computes nyquist_compliant correctly. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate validation into state.py wave completion hook</name>
  <files>scripts/state.py</files>
  <action>
    In scripts/state.py, modify check_auto_advance() to call nyquist validation when all plans in a wave complete:

    1. Add import at top: `from scripts.nyquist import run_wave_validation, update_validation_frontmatter`
    2. In check_auto_advance(), after the existing "All plans done" block (around line 640), add wave validation logic:
       - Get the phase's slug from the phase dict
       - Construct phase_dir as Path(f".planning/phases/{phase['sequence']:02d}-{phase['slug']}")
         (use phase["sequence"] for the zero-padded number, phase["name"] or derive slug from phase name)
       - Call run_wave_validation(phase_dir, wave=max_wave) where max_wave is the highest wave number among completed plans
       - Call update_validation_frontmatter(phase_dir, wave_result) to persist results
       - Add validation result info to the returned dict: result["validation"] = wave_result

    3. Handle errors gracefully: wrap nyquist calls in try/except. If VALIDATION.md is missing or malformed, log a warning in the result dict but do NOT block phase advancement. Validation is informational per user decision.

    4. Important: Do NOT change the existing phase transition logic. Validation runs as a side effect, not a gate.

    Verify the phase directory path construction by checking how existing code references phase directories (grep for ".planning/phases" in the codebase).
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/meridian && uv run pytest tests/test_state.py tests/test_nyquist.py -x -v</automated>
  </verify>
  <done>check_auto_advance() calls run_wave_validation and update_validation_frontmatter after all plans complete. Validation failures are logged but do not block phase transitions. Existing state.py tests still pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_nyquist.py -x -v` — all nyquist engine tests pass
2. `uv run pytest tests/test_state.py -x -v` — existing state tests still pass (no regression)
3. `ruff check scripts/nyquist.py scripts/state.py --select E501` — no lint violations
</verification>

<success_criteria>
- scripts/nyquist.py exists with parse_validation_md, run_wave_validation, update_validation_frontmatter
- VALIDATION.md frontmatter is updated with per-wave results when plans complete
- nyquist_compliant computed correctly (true only when all waves pass)
- status transitions from "draft" to "validated" or "failed"
- No regression in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-nyquist-compliance/06-01-SUMMARY.md`
</output>
