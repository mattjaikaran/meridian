---
phase: 09-quality-gates
plan: 04
type: feature
wave: 2
depends_on: []
files_modified:
  - scripts/audit.py
  - skills/audit-uat/SKILL.md
  - tests/test_audit.py
autonomous: true
requirements:
  - GATE-04

must_haves:
  truths:
    - "/meridian:audit-uat scans all phases for outstanding verification items"
    - "Collects unchecked items from VALIDATION.md sign-off sections"
    - "Collects manual verification items that haven't been confirmed"
    - "Produces a summary report grouped by phase with actionable items"
    - "Exit code reflects whether any verification debt exists"
  artifacts:
    - path: "scripts/audit.py"
      provides: "UAT audit scanning across all phases"
      exports:
        - "audit_uat"
        - "collect_verification_debt"
    - path: "skills/audit-uat/SKILL.md"
      provides: "/meridian:audit-uat skill definition"
    - path: "tests/test_audit.py"
      provides: "Unit tests for UAT audit"
      min_lines: 40
---

<objective>
Build /meridian:audit-uat for cross-phase verification debt tracking.

Scans all phase directories for outstanding UAT items:
1. Parse each phase's VALIDATION.md for unchecked sign-off items (`- [ ]`)
2. Parse each phase's VERIFICATION.md for human_verification items not yet confirmed
3. Check each phase's must_haves truths against actual verification status
4. Aggregate into a report:
   - Phase X: 2 unchecked sign-off items, 1 pending human verification
   - Phase Y: all clear
5. Return structured dict + formatted markdown report
</objective>

<tasks>
- task: Create `scripts/audit.py` with `collect_verification_debt()` and `audit_uat()`
  wave: 2
  verify: "uv run pytest tests/test_audit.py -x"

- task: Create `skills/audit-uat/SKILL.md` with command definition
  wave: 2
  verify: "test -f skills/audit-uat/SKILL.md"

- task: Write tests with phases that have debt and phases that are clean
  wave: 2
  verify: "uv run pytest tests/test_audit.py -v"
</tasks>
