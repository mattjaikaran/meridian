---
phase: 13-execution-resilience
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - scripts/executor_modes.py
  - tests/test_executor_modes.py
autonomous: true
requirements:
  - EXEC-01

must_haves:
  truths:
    - "Interactive executor mode pauses after each task for user review"
    - "User can approve, reject, or modify before proceeding to next task"
    - "Rejected tasks are marked for re-execution with feedback"
    - "Mode is activated with --interactive flag on /meridian:execute"
  artifacts:
    - path: "scripts/executor_modes.py"
      provides: "Execution mode logic — interactive and autonomous"
      exports:
        - "InteractiveExecutor"
        - "should_pause_for_review"
        - "format_task_review_prompt"
    - path: "tests/test_executor_modes.py"
      provides: "Unit tests for executor modes"
      min_lines: 40
---

<objective>
Build interactive executor mode for pair-programming style execution.

When --interactive is passed to /meridian:execute:
1. Execute one plan at a time (no parallel waves)
2. After each plan completes, show: files changed, tests run, commit made
3. Pause and ask user: [A]pprove / [R]eject / [M]odify
4. Approve: proceed to next plan
5. Reject: mark plan as failed with user feedback, continue or stop
6. Modify: user provides additional instructions, re-run plan

This gives the user a chance to review each piece of work before moving on,
useful for critical or unfamiliar codebases.
</objective>

<tasks>
- task: Create `scripts/executor_modes.py` with InteractiveExecutor class
  wave: 1
  verify: "uv run pytest tests/test_executor_modes.py -x"

- task: Write tests for review prompt generation and mode logic
  wave: 1
  verify: "uv run pytest tests/test_executor_modes.py -v"
</tasks>
